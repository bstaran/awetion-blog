import { siteConfig } from '@/lib/siteConfig'

const config = siteConfig({
  rootNotionPageId: '',
  domain: '',
  title: '',
  description: '',
  mainPageTitle: '',
  homeLinkTitle: 'blog',
})

export default config
